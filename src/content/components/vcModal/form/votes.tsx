import React from 'react';
import { ReducerProps } from '../form';

export function VotesTab({ state: _state, dispatch: _dispatch }: ReducerProps) {
	return (
		<section className="grow w-full gap-2 flex flex-col">
			<div>Votes Tab</div>
		</section>
	);
}
